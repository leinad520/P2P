import React from 'react';
import { render } from '@testing-library/react';
import AddToCart from './AddToCart.jsx';

describe('AddToCart', () => {
  test('render AddToCart', () => {
    // render(<AddToCart />);

    // screen.debug();
  })
})